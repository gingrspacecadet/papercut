import { BaseStep } from '../baseStep.js';

export default class extends BaseStep {
    render() {
        return `
            <h1>Detecting Kindle..</h1>
            <p>beep* boop* bop.</p>
        `;
    };
};